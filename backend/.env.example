# Copy this file to .env and fill in real values. Never commit .env.

# --- App ---
APP_ENV=development
LOG_LEVEL=INFO
TIMEZONE=UTC
EXCHANGE_TIMEZONE=America/New_York

# --- Auth ---
JWT_SECRET=change-me-to-a-long-random-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=720
OPERATOR_USERNAME=admin
OPERATOR_PASSWORD=change-me

# --- Database (Neon Postgres) ---
# Neon connection strings normally end with ?sslmode=require - keep that.
# Use the asyncpg driver form: postgresql+asyncpg://...
DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@HOST/DBNAME?ssl=require

# --- Broker + Market Data: Bybit TradFi ---
# Bybit supplies both execution and market data - TradFi (US stocks/ETFs)
# trades as USDT-settled linear perpetuals on the same V5 API as crypto.
BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_ENV=demo
# BYBIT_BASE_URL / BYBIT_WS_URL let you point at demo vs live without code changes.
BYBIT_BASE_URL=https://api-demo.bybit.com
BYBIT_WS_URL=wss://stream-demo.bybit.com/v5/private
# Public market data (klines/tickers) is the same feed on demo or live, so
# this always points at the mainnet public stream.
BYBIT_PUBLIC_WS_URL=wss://stream.bybit.com/v5/public/linear
BYBIT_RECV_WINDOW=5000

# --- Risk Defaults (Semi Aggressive profile) ---
RISK_PER_TRADE_PCT=1.25
DAILY_LOSS_LIMIT_PCT=4.0
MAX_CONCURRENT_POSITIONS=4
MAX_PORTFOLIO_EXPOSURE_PCT=80.0
MAX_STRATEGY_ALLOCATION_PCT=50.0
MAX_SPREAD_PCT=0.15

# --- CORS ---
DASHBOARD_ORIGIN=http://localhost:3000
